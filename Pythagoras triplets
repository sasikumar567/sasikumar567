#include<stdio.h>
#include<math.h>
int main()
{
	int m,a,b;
	float c;
	printf("enter the limits:");
	scanf("%d",&m);
	for(a=1;a<m;a++)
	{
		for(b=1;b<m;b++)
		{
			c=sqrt((a*a)+(b*b));
			if(c==int(c) and a<=b)
			{
				printf("%2d,%2d,%2d\n",a,b,int(c));
			}
	
		}
   }

}
